[tool.black]
# un.py is our vendored Unsandbox SDK (refreshed with curl, see CLAUDE.md)
# & migrations/ are generated by Alembic: keep both byte-for-byte upstream.
extend-exclude = '''
^/(un\.py|migrations/)
'''
